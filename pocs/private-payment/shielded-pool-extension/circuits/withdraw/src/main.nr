// Withdraw spend circuit (extended).
//
// Same diff as transfer: recursive chain-proof verification per input,
// commitment-tree membership, public per-input nullifier derivation, output
// commitment binds `epoch_created`. No active-tree work (the insertion circuit
// handles that). See ../../SPEC.md.
//
// TODO: implement.

fn main() {}
