// Deposit circuit (extended).
//
// Diff vs parent shielded-pool: commitment binds `epoch_created`, and the
// circuit enforces `epoch_created == currentEpoch` (passed as a public input
// from the contract). See ../../SPEC.md "Deposit Circuit (diff)".
//
// TODO: implement.

fn main() {}
