// Off-chain indexed Merkle tree: sorted-low-leaf insertion, predecessor
// lookup, root recomputation. Mirrors the in-circuit construction so the
// relayer can prepare insertion witnesses for the insertion circuit, and the
// chain-update circuit's non-membership witnesses can be built off frozen
// trees.
//
// TODO: implement.
